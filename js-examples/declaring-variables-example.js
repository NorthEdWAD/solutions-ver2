// Abraham Lincoln
// 09 APR 20XX
// Variables Example

// Declare (create) variables, then assign a value to each variable
// Use const to ensure you can't overwrite the value stored in a variable by mistake

// Mike and Jenkins are string data (text data)
const firstName = "Mike";
const lastName = "Jenkins";
// 21 is an example of numeric data, i.e. a number
const age = 21;

// Use keyword let to create a variable whose value CAN be overwritten
// Player's score can be updated (overwritten) as the player plays the game
let playerScore = 0;
